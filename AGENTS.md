# 项目协作规范

## 文档语言

- 今后在本仓库中新增或修改的所有 Markdown 文档，正文必须使用中文。
- 代码、命令、文件路径、接口名、类型名、依赖名和其他技术标识符可以保留英文原文。
- 引用外部英文资料时，应优先补充中文说明；不得无说明地使用大段英文作为文档正文。

## 任务执行方式

- 只有当用户明确提出“给个方案”“设计一下”“设计文档”“先规划”“先讨论方案”或同等含义的要求时，才进入方案设计、设计文档或分阶段确认流程。
- 用户未明确要求设计时，默认直接执行：先用简短文字说明准备修改的内容和范围，然后立即检查代码、完成修改并运行必要验证，不得等待额外确认。
- 默认执行流程中的修改说明应简洁、具体，不得展开多套备选方案、撰写额外设计文档或设置不必要的审批关卡。
- 只有缺少的信息会显著改变实现结果，或操作具有不可逆、高风险影响时，才允许在执行前提出澄清问题。
- 代码修改完成后，应直接说明实际变更、验证结果和仍存在的客观限制，不得重复叙述冗长的思考过程。

## 执行纠错与经验沉淀

### 基本要求

- 执行过程中一旦出现误操作、无效尝试、工具调用失败或因判断错误造成的返工，必须先确认根因，再继续操作；不得在未检查实际文件状态的情况下重复执行同一命令。
- 每次任务结束前，必须判断本次是否产生了新的、可复用的纠错经验；如有，必须在本节补充或合并对应规则后再交付结果。
- 记录内容必须包含问题场景、根因和以后必须执行的解决办法。相同问题应合并更新，避免将本文件写成重复的时间流水账。
- 只记录适用于本仓库或当前开发环境的可执行规则，不记录密钥、令牌、个人信息、临时目录名称和一次性进程标识。
- 用户未明确要求方案设计时，应优先完成代码修改与验证。除非存在会显著改变结果的歧义或高风险操作，不得增加不必要的设计文档、确认关卡或长时间停留在方案说明阶段。

### 已知问题与强制措施

| 问题场景 | 根因 | 以后必须执行的解决办法 |
| --- | --- | --- |
| `apply_patch` 被中断、超时或返回状态不明确 | 工具调用可能已经部分写入文件，直接重试会造成重复内容或上下文冲突 | 立即读取目标文件并执行 `git diff -- <path>` 确认实际落盘状态；只补充缺失部分，不得假设补丁完全成功或完全失败 |
| 大补丁提示找不到预期上下文 | 文件内容、换行符或并发修改与补丁上下文不一致 | 重新读取最新文件，改用仓库相对路径，并将补丁拆成逐文件、较小上下文的修改；不得反复提交同一个大补丁 |
| PowerShell 将 Maven 的 `-D` 参数或包含逗号的测试列表错误解析 | PowerShell 原生命令参数解析会拆分未正确引用的参数 | 每个 Maven 系统属性参数必须整体加引号，例如 `mvn '-Dtest=ApiResponseTest,GlobalExceptionHandlerTest' test`；执行前先检查命令是否包含逗号、美元符号或 PowerShell 特殊字符 |
| Maven 无法写入现有 `target` 目录 | `target` 中的资源或状态文件可能被其他进程占用，或目录权限异常 | 先确认失败点，不得直接删除或清空 `target`；改用独立的临时构建目录进行验证。若为此临时修改 `pom.xml`，测试结束后必须精确撤销临时配置，并用 `git diff` 和特征文本搜索确认没有残留 |
| 新增测试文件没有出现在 `git status` | `.gitignore` 当前忽略 `src/test/`，新测试可能只存在于本地而无法随变更交付 | 创建测试文件前先执行 `git check-ignore -v <path>`；若路径被忽略，应选择已跟踪的测试文件、明确调整忽略规则，或向用户说明限制，不得默默留下无法交付的测试 |
| 中文源码或测试内容在终端显示为乱码 | PowerShell 默认编码与文件 UTF-8 编码不一致 | 读取中文文件时显式使用 `Get-Content -Encoding UTF8`；修改后通过编译或 UTF-8 读取再次确认字符串和引号完整，不得仅凭默认终端显示判断文件已损坏 |
| 将多个检查放在同一次组合调用后，只看到最后一个失败结果 | `rg` 未匹配等正常的非零退出码可能让整组调用显示失败并遮蔽其他结果 | 对可能返回非零的检查单独执行，或显式分别收集每条命令的输出与退出码；关键验证结果不得依赖一个容易被无关失败覆盖的组合调用 |
| 全量测试出现与当前改动无关的既有失败 | 仓库基线可能已经存在失败用例，直接归因于当前修改会导致错误返工 | 先运行本次改动的定向测试，再运行全量测试；分别记录两者结果，通过失败文件、断言和变更范围判断关联性，不得擅自修改无关模块来追求全绿 |
| 工作区已有用户未提交修改 | 在脏工作区中执行回退或批量格式化可能覆盖用户工作 | 修改前执行 `git status --short`；只编辑任务涉及的文件，撤销临时变更时使用精确补丁，最终再次检查状态，不得使用 `git reset --hard`、`git checkout --` 或其他批量回退命令 |
| 跨模块迁移删除导入后出现干净构建失败 | 同一文件同时包含被迁移模块和本模块仍使用的查询导入，批量删除时误删后者 | 删除导入后立即执行定向编译，并逐个搜索该文件仍使用的类型；提交前检查 `git diff` 与干净工作区状态，避免把补救导入留在未提交修改中 |

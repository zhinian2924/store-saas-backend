spring:
  datasource:
    url: jdbc:mysql://192.168.85.202:3306/store_saas?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: admin123
  data:
    redis:
      host: 192.168.85.202
      port: 6379
      password: admin123

storage:
  minio:
    enabled: true
    endpoint: http://192.168.85.202:9000
    access-key: admin
    secret-key: admin123
    bucket: store-images

logging:
  level:
    root: warn   # 设置根日志级别为warn，可选值有trace, debug, info, warn, error, fatal
    com.example.storesaas: debug

miniapp:
  # 仅供本地开发。生产环境必须通过MINIAPP_SECRET_KEY提供独立的32字节Base64密钥。
  secret-key: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
